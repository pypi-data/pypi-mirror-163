from setuptools import setup

setup(name='useful_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['useful_distributions'],
      author=['Kianna Hendricks'],
      zip_safe=False)
