[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]

[project]
description = "USGS Earthquake Impact Utilities for Colors"
name = "esi_utils_colors"
requires-python = ">=3.8"
version = "1.0.2"
dependencies = [
  "numpy",
  "matplotlib",
  "pandas",
]
