[tool.poetry]
name = "muclearn"
version = "0.1.2.post2"
description = "A collection of helpers and tools for machine learning."
authors = ["Fabian Reinold <fabian.reinold@muenchen.de>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2"
pandas = "^1.4"
scikit-learn = "^1"
holidays = "^0.14.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
