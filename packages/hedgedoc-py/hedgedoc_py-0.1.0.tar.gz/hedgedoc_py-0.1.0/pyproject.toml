[tool.poetry]
name = "hedgedoc_py"
version = "0.1.0"
description = "Simple wrapper around the HedgeDoc API"
authors = ["JulianSobott <julian.sobott@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.1"

[tool.poetry.dev-dependencies]
black = "^22.6.0"
pytest = "^7.1.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
