============
Installation
============

At the command line::

    $ easy_install django-xperms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-xperms
    $ pip install django-xperms
