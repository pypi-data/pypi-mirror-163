[tool.poetry]
name = "selenium_setup"
#
authors = [" "]
description = ""
readme = "README.md"
repository = "https://github.com/m9810223/selenium_setup"
#
packages = [
  {include = "selenium_setup", from = "src"},
]
#
version = "0.0.0"

[tool.poetry.dependencies]
python = ">=3.7,<4"
#
selenium = "*"
#
httpx = "^0.23.0"
rich = "^12.5.1"
typer = "^0.6.1"

[tool.poetry.dev-dependencies]

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]
