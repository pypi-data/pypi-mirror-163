from setuptools import setup

setup(
  name = 'pyDataViz',
  version = '0.0.1',
  description = 'visualizaing data by Amir H.',
  py_modules = ['histogram'],
  package_dir = {'': 'src'},
)

# name what what you will use for pip install
